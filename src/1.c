#include <stdio.h>
#include <stdlib.h>

int main() {
  int age = 25;
  if (age >= 18) {
    printf("You are eligible to vote.");
  } else {
    printf("Sorry, you are not eligible to vote.");
  }
  return 0;
}
